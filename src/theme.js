import { createTheme } from "@material-ui/core/";

const theme = createTheme({
  palette: {
    type: "dark",
    primary: {
      main: "#2196f3",
    },
    secondary: {
      main: "#f44336",
    },
  },
});

export default theme;
